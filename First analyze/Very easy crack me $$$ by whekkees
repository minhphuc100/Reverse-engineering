In https://crackmes.one/lasts/
Download link https://crackmes.one/download/crackme/69b18b47ddd6176826ae8950

# Binary Analysis: crackme4.exe
**Date**: 3/11/2026
**Objective**: The first analyze to do as the work follow 
## Initial Reconnaissance
- File type: PE32+ executable
- Architecture: window x86-64
- Stripped: Yes ? ( Program Database (.pdb) file exists in the same folder as the .exe when it is not stripped ? )
- Security features: Yes ?
## Key Functions
- main: [text:00000001400012A0] - [unknown]
- sub_140001630: [renamed to printout] - [is std::cout]
- sub_140001850: renamed to cin_funtion - is cin
- memcmp : compare the values
## Interesting Strings
- "Please enter ur login:": used at the start to get the first login
- Please enter ur password: the second one
- "Nice job bro": printed inside a conditional
## Current Hypothesis
What I think is happening
we input username and password, then, it will compare with some value to check for the conditional

The memcmp function in C is used to compare two blocks of memory byte by byte.
int memcmp(const void *s1, const void *s2, size_t n);
In the file, two condition we need to look after
if ( Size == v7 && (!Size || !memcmp(v8, Buf2, Size)) ) to see the next input
and
if ( v21 == v6 && (!v21 || !memcmp(v12, v18, v21)) ) to complete

the first input is "whekkes"
the second input is "qwerty"
( we can run by using cmd to run the crackmes.exe to see the final value)
Reason:
In psuedo code, i saw strcpy function, which assume to put value ( strings into some variable) ( i had look at it before)
which the first is buf2 with "whekkes", second, v18 with  "qwerty"

Then i just assume they are two input value ?
Maybe i should look at it deeper.
in the first cmpmem function, we compare v8 and buf2, but upper, we has v8=buf1 right after the cin for buf1

the second one is the same
we compare v12 and v18, which, v12=Block right after the cin for Block
-> Which these two conditionals is only thing we can affect in this program, so i assume this is the correct answer.

However i still don't understand many thing in there, like:
- Why there is a loop here ? (v7++)
- Why compare v25 (with value 15) with 0xF for v8=(void **) buf1[0]
and many more ...

But i think put that aside and move to the next one.



