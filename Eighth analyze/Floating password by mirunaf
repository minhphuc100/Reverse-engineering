# Floating password - Reverse Engineering Writeup

**Difficulty:** Beginner
**Platform:** Windows PE32
**Language:** C / C++ 
**Tools Used:** IDA Free

---

## 1. Introduction


### Objective
find password

---

## 2. Initial Reconnaissance

### 2.1 File Identification

Basic information about the target binary:

```txt
Filename: crackme
Size: 
Type: PE32
Architecture: x86 / x64 


2.2 Basic Execution Test ( i don;t run it on window bc defender delete it instantly :v)

## 3. Static Analysis
3.1 Binary Structure Analysis

Analyze executable sections and overall layout.

Section  	Virtual Address	  Raw Offset	Purpose
.text    	[addr]	          [offset]	  Executable code
.rdata  	[addr]  	        [offset]	  Read-only data
.data	    [addr]  	        [offset]	  Initialized data
.rsrc    	[addr]	          [offset]	  Resources
.reloc   	[addr]  	        [offset]	  Relocations

3.2 String Analysis

Search for readable strings using:

strings
.rdata
.data
Unicode strings
Cross-references
Address	  String	    Notes
[addr]	  [string]	  [purpose]



3.3 Function Analysis

Identify important functions.

Function (inside main)                                                         Purpose

std::operator<<<std::char_traits<char>>(refptr__ZSt4cout, "So the password is? : ")  out and edit the refptr__ZSt4cout
std::istream::operator>>(refptr__ZSt3cin, &he)                                       do st with refptr__ZSt4cout ( the refptr have no purpose ?)
ko()                                                                                 the wrong output

####still cann't found where is the input ?

In ko()

we have kol() funtion where it check compare he and h, if same, koj() else, print the "wrong" output

where he is in the second upper parameter, and h is h = ((v7 * v6) / 67745.0 * v7) + 67745.0


After look at write up:
"
The password is dynamically generated after you type in your input. 
It's calculated on the fly using std::random_device + _time64 and a formula based on the float constant 67745.0. 
Since the check is non-deterministic and run after input, you can't guess it statically. 
Plus, the binary intentionally crashes at the end with a UD2 instruction to prevent the stdout buffer from flushing "ok, that's it" out
To solve it, I patched the binary:
1. In `kol()`, NOP'ed out the conditional jumps (JP at 0xd8E and JNZ at 0xd93) to force the success branch.
2. Patched the UD2 crash at 0xebc with a clean stack cleanup &amp; return:
ADD RSP, 0x13d8 (48 81 C4 D8 13 00 00)
POP RBX (5B), POP RBP (5D), RET (C3)"

I don't understand a thing, but, i thought it is about editing the code ?

Inconclusion ?
Skip the check, jump to the correct path right away
Patched the crash



