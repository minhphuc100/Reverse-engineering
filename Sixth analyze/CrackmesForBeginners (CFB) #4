# [CrackmesForBeginners (CFB) #4] - Reverse Engineering Writeup

**Difficulty:** 2.0
**Platform:** Windows PE32
**Language:** C / C++ 
**Tools Used:** [IDA Free x64dbg, strings

---

## 1. Introduction

### Objective
take 13 characters password

## 2. Initial Reconnaissance

### 2.1 File Identification

Basic information about the target binary:

```txt
Filename: CFB4.exe
Size: 263680
Type: PE32
Architecture:x64

2.2 Basic Execution Test

In the jpg

Program asks for user input, then validitating it.



## 3. Static Analysis
3.1 Binary Structure Analysis
no need for this file, the input is encrypted right away, and compare to st -> this is more like crypto but in re

3.2 String Analysis

as explain above, the password is not found in string, the structure of the file is not very complex, the main thing is the encrypt function

3.3 Function Analysis

The main : loc_1400060FF

At the final condition, it compare with DF(h)
So technically, we can reverse each step to find the final password or change the flag by add breakpoint very easily







