In https://crackmes.one/lasts/
Download link https://crackmes.one/crackme/69bc0fb3ddd6176826ae8a55

# Binary Analysis: CTF_Level5.exe
**Date**: 3/19/2026
**Objective**: Capture the flag with format CTF_{...} 
## Initial Reconnaissance
- File type: executable
- Architecture: window x86-64
- Stripped: Yes 
- Security features: Yes
## Key Functions
- main: text:00000001400016E6
- Puts ( printf with \n)
- __imp___acrt_iob_func from api-ms-win-crt-stdio-11-1-0.dll
- sub_1400028F0
- fgets 
- sub_1400015B8 as the_main_condi
- 
## Interesting Strings
- Even with random input, it still output "Correct". But inside we also have "Wrong" output
- 
-
## Current Hypothesis
- I thought it is messing with us, right before the jump, we have test eax, eax
and
jnz ... to correct and wrong output
right after a function sub_1400015B8

( actually, the input of 1 and 2 is accidentally correct the condition inside, but with ctf{11111}, it output with "wrong!!"

- function tracking
Fgets : char *fgets(char *str, int n, FILE *stream);
Behavior: Reads until a newline (\n) is hit, n-1 characters are read, or the end of the file (EOF) is reached. It automatically adds a null terminator (\0) at the end.
Assembly Return: Just like puts, it returns a pointer to the buffer in the RAX/EAX register on success, or NULL (0) on failure.

in this case
v0 = __acrt_iob_func(0);
fgets(Buffer, 64, v0);

Now that the program has the pointer to stdin (v0), it calls fgets to actually get your typing.
Buffer: The memory location where your input will be saved.
64: The maximum number of bytes to read. This is a security feature to prevent a "Buffer Overflow"—it will never read more than 63 characters (leaving 1 byte for the null terminator).
v0: Tells fgets to read from the keyboard ( the above function ).

the strlen and if condition: is the newline strip (\n is 10 in ascii)

Now the the main condi

malloc: as learnt before, it "not" save data to the heap,it create a available space in heap ,then return the address of the data to rax
Request      mov rcx, 64       ;   Passing the size to the function.

shl: shift logical left, here:
shl eax,3 is mul-t eax with 8

CDQE (Convert Doubleword to Quadword Extended) is an x86-64 assembly instruction used to sign-extend a value from 32 bits to 64 bits.It specifically operates on the RAX register.
It takes the 32-bit value in EAX and fills the upper 32 bits of RAX with the "sign bit" (the highest bit of EAX).

* is sometime multi, is look at address of variable and take data from it, or
Pseudo-code: *(_BYTE *)ptr
Assembly Translation: This forces the assembly to use a specific size suffix or register size.
x86: mov al, byte ptr [ebx]   (The _BYTE cast tells the assembler to only use the 8-bit al register, and the * outside tell to look at that address to get value).

found but don't know why again.





